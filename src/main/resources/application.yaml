spring:
  application:
    name: devops-web-service
  datasource:
    url: jdbc:postgresql://localhost:5432/web-db
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080
